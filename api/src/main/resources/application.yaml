spring:
  application:
    name: nestspend-api
  profiles:
    active: dev

server:
  port: 8080

# JWT defaults - override via environment variables in production:
# JWT_SECRET and JWT_EXPIRATION_MS
jwt:
  secret: ${JWT_SECRET:}
  expiration-ms: ${JWT_EXPIRATION_MS:86400000}
